apiVersion: v1
kind: Service
metadata:
  labels:
    app: dev-fivem-deployment
  name: dev-fivem-svc-a
  namespace: fivem
spec:
  ports:
  - name: fivem
    port: 30120
    protocol: UDP
    targetPort: 30120
  selector:
    app: dev-fivem-deployment
  type: ClusterIP