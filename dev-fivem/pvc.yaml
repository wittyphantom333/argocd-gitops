kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: dev-fivem-workspace-001-data
  namespace: fivem
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 16Gi