apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "harbor.fullname" . }}-test-connection"
  labels:
    {{- include "harbor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "harbor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
