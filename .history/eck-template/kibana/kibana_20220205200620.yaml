apiVersion: kibana.k8s.elastic.co/v1
kind: Kibana
metadata:
  name: ECK_NAME
spec:
  version: ECK_VERSION
  count: 1
  http:
    service:
      spec:
        type: ClusterIP  
  elasticsearchRef:
    name: elasticsearch
  enterpriseSearchRef:
    name: enterprise-search   