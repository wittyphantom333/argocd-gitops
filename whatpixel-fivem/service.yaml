apiVersion: v1
kind: Service
metadata:
  labels:
    app: dev-fivem-ns
  name: dev-fivem-ns
  namespace: fivem
spec:
  ports:
  - name: fivem
    port: 30120
    protocol: UDP
    targetPort: 30120
  selector:
    app: dev-fivem-deployment
  type: LoadBalancer