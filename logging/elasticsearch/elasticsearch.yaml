apiVersion: elasticsearch.k8s.elastic.co/v1
kind: Elasticsearch
metadata:
  name: elasticsearch
  annotations:
    elasticsearch.alpha.elastic.co/autoscaling-spec: |
      {
          "policies": [{
              "name": "di",
              "roles": ["data", "ingest" , "transform", "remote_cluster_client"],
              "deciders": {
                "proactive_storage": {
                    "forecast_window": "5m"
                }
              },
              "resources": {
                  "nodeCount": { "min": 3, "max": 6 },
                  "cpu": { "min": 2, "max": 8 },
                  "memory": { "min": "2Gi", "max": "6Gi" },
                  "storage": { "min": "50Gi", "max": "100Gi" }
              }
          },
          {
              "name": "ml",
              "roles": ["ml", "remote_cluster_client"],
              "deciders": {
                  "ml": {
                      "down_scale_delay": "10m"
                  }
              },
              "resources": {
                  "nodeCount": { "min": 1, "max": 3 },
                  "cpu": { "min": 1, "max": 4 },
                  "memory": { "min": "2Gi", "max": "8Gi" },
                  "storage": { "min": "10Gi", "max": "50Gi" }
              }
          }]
      }
spec:
  version: 7.17.0
  nodeSets:
    - name: master
      count: 1
      config:
        xpack.monitoring.collection.enabled: true
        node:
          roles: [ "master" ]
          store.allow_mmap: false
    - name: di
      config:
        xpack.monitoring.collection.enabled: true
        node:
          roles: [ "data", "ingest", "transform", "remote_cluster_client" ]
          store.allow_mmap: false
    - name: ml
      config:
        xpack.monitoring.collection.enabled: true
        node:
          roles: [ "ml", "remote_cluster_client" ]
          store.allow_mmap: false
